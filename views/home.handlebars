<main>
        <section class="tarefas">
                {{#each tarefas}}
            <div class="tarefa">
                <form action="">
                        <div>
                        <input type="checkbox" name="" id="tarefa-{{this.id}}">
                        <label for="tarefa-{{this.id}}">{{this.descricao}}</label>
                        </div>
                    <button>
                        <img src="/imagens/x_icon.png" alt="icone com formato da letra X">
                    </button>
                    
                    
                </form>
            </div>
                {{/each}}   
                
            <div class="informacoes">
                <p>5 tarefas ativas</p>

                <div class="botoes">
                    
                    <a href="" class="active">Todas</a>
                    <a href="">Ativas</a>
                    <a href="">Completas</a>
                </div>
                <a href="">Limpar tarefas</a>
            </div>
        </section>

        <section class="botoes">

            
            <a href="" class="active">Todas</a>
            <a href="">Ativas</a>
            <a href="">Completas</a>

        </section>
</main>